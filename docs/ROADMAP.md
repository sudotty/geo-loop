# Roadmap

The primary roadmap is maintained at the repository root:

```text
ROADMAP.md
```

Use the root roadmap as the single source of truth for execution phases, MVP priorities, issue generation, agent PR workflow, GEO visibility tracking, ASO loop and hosted product planning.

This file exists because the README planned repository structure includes `docs/ROADMAP.md`. Keeping this pointer avoids duplicate roadmap drift.
